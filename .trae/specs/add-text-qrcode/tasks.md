# Tasks

- [x] Task 1: 添加输入模式切换 UI 组件
  - 在输入框上方添加"URL"和"文字"两个模式切换标签按钮
  - 实现标签切换的 CSS 样式（选中态/非选中态）
  - 实现点击切换逻辑，存储当前模式状态
  - **验证**：点击两个标签可正常切换，选中态样式正确

- [x] Task 2: 更新页面标题和文案
  - 将页面标题从"URL 转二维码"改为"二维码生成器"
  - 更新 `<title>` 标签内容
  - 更新输入框 label 文案以适配当前模式
  - 更新二维码下方提示文案
  - **验证**：页面标题显示正确，切换模式时输入框提示语跟随变化

- [x] Task 3: 更新二维码生成逻辑
  - 修改 `generateQR` 函数，根据当前模式处理输入内容
  - URL 模式下保持现有逻辑不变
  - 文字模式下不校验 URL 格式，直接将文字内容传入 QRCode
  - **验证**：URL 模式和文字模式均能正确生成二维码

- [x] Task 4: 更新二维码区域下方显示内容
  - URL 模式下保留链接显示
  - 文字模式下显示原始文字内容
  - **验证**：两个模式下内容显示区域均正确展示对应内容

# Task Dependencies
- Task 2 依赖 Task 1（需要先有模式状态才能根据模式切换文案）
- Task 3 依赖 Task 1（需要先有模式状态才能判断生成逻辑）
- Task 4 依赖 Task 3（显示内容与生成逻辑强相关）
- Task 2 和 Task 3 可并行进行
